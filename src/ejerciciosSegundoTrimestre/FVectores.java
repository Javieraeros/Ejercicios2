package ejerciciosSegundoTrimestre;
public class FVectores {
	
	/* 
	 * Interfaz 
	 * Cabecera:Vector sumaVector(Vector sumar)
	 * Proceso: Calcula la suma de dos vectores
	 * Precondiciones: Lso vectores no pueden ser complementarios
	 * Entrada:
	 * Salida:
	 * Entrada/Salida:
	 * Postcondiciones:
	 */
	
	/*public Vector sumaVector(Vector sumar)throws IllegalArgumentException{
		if(x==-sumar.getX() && y==-sumar.getY() && z==-sumar.getZ()){
			throw new IllegalArgumentException("Error, no puedes sumar el vector contrario");
		}else{
			this.x=x+sumar.getX();
			this.y=y+sumar.getY();
			this.z=z+sumar.getZ();
		}
	}*/
	
	/* 
	 * Interfaz 
	 * Cabecera:
	 * Proceso
	 * Precondiciones:
	 * Entrada:
	 * Salida:
	 * Entrada/Salida:
	 * Postcondiciones:
	 */
	//public void 
}

